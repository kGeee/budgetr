{
  "name": "@budgetr/relay",
  "version": "0.1.0",
  "private": true,
  "description": "budgetr companion — content-blind ciphertext relay (spec §6)",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  }
}
