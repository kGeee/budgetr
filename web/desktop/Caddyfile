budgetr.dev {
	# budgetr.dev resolves only via /etc/hosts, so public ACME (Let's Encrypt)
	# can't validate it — use Caddy's locally-trusted internal CA instead.
	tls internal
	reverse_proxy localhost:3000
}
